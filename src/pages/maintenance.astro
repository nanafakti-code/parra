---
// src/pages/maintenance.astro
// Página de mantenimiento
---

<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Mantenimiento | Volveremos pronto</title>
        <meta
            name="description"
            content="Estamos trabajando en mejoras importantes. Volveremos muy pronto."
        />

        <!-- Preconexión para fuentes y carga de Inter -->
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
            rel="stylesheet"
        />
    </head>
    <body>
        <div class="maintenance-container">
            <!-- Efecto de iluminación ambiental tipo cloud -->
            <div class="glow-effect"></div>

            <div class="content">
                <!-- Logo (Sustituye el src por tu logo real de RBV, ej: /images/logo.svg) -->
                <div class="logo-container fade-in">
                    <!-- Fallback a SVG si no se encuentra la imagen -->
                    <img
                        src="/logo.svg"
                        alt="Logo"
                        class="floating-logo"
                        onerror="this.src='data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHZpZXdCb3g9IjAgMCAyMDAgNjAiPjx0ZXh0IHk9IjQ1IiBmb250LWZhbWlseT0iSW50ZXIiIGZvbnQtc2l6ZT0iNDAiIGZvbnQtd2VpZ2h0PSJib2xkIiBmaWxsPSIjZmZmIiBsZXR0ZXItc3BhY2luZz0iLTEiPlJCVjwvdGV4dD48L3N2Zz4='"
                    />
                </div>

                <main class="main-content fade-in-up delay-1">
                    <h1 class="gradient-text">
                        Estamos mejorando la experiencia
                    </h1>
                    <p class="description">
                        Nuestro equipo está trabajando en mejoras importantes
                        para ofrecerte el mejor servicio. Volveremos muy pronto.
                    </p>

                    <!-- Barra de Progreso -->
                    <div class="progress-section fade-in-up delay-2">
                        <div class="progress-header">
                            <span class="progress-label"
                                >Actualización en curso</span
                            >
                            <span class="progress-percentage">70%</span>
                        </div>
                        <div class="progress-track">
                            <div class="progress-bar"></div>
                        </div>
                    </div>

                    <!-- Botones de Acción -->
                    <div class="actions fade-in-up delay-3">
                        <a
                            href="https://wa.me/tunumerodewhatsapp"
                            target="_blank"
                            rel="noopener noreferrer"
                            class="btn btn-glass"
                        >
                            <svg
                                viewBox="0 0 24 24"
                                width="20"
                                height="20"
                                stroke="currentColor"
                                stroke-width="2"
                                fill="none"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                ><path
                                    d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"
                                ></path></svg
                            >
                            WhatsApp
                        </a>
                        <a
                            href="mailto:contacto@tuemail.com"
                            class="btn btn-glass"
                        >
                            <svg
                                viewBox="0 0 24 24"
                                width="20"
                                height="20"
                                stroke="currentColor"
                                stroke-width="2"
                                fill="none"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                ><path
                                    d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"
                                ></path><polyline points="22,6 12,13 2,6"
                                ></polyline></svg
                            >
                            Email
                        </a>
                        <!-- El botón volver al inicio puede ir a twitter, instagram o simplemente recargar -->
                        <a href="/" class="btn btn-primary">
                            <svg
                                viewBox="0 0 24 24"
                                width="20"
                                height="20"
                                stroke="currentColor"
                                stroke-width="2"
                                fill="none"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                ><path
                                    d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"
                                ></path><polyline points="9 22 9 12 15 12 15 22"
                                ></polyline></svg
                            >
                            Volver al inicio
                        </a>
                    </div>
                </main>
            </div>
        </div>
    </body>
</html>

<style>
    /* Variables y Sistema de Diseño Moderno */
    :root {
        --bg-color: #050a14;
        --brand-blue: #00f0ff;
        --brand-purple: #7000ff;
        --brand-teal: #00b0ff;

        --text-pure: #ffffff;
        --text-muted: #94a3b8;

        --glass-bg: rgba(255, 255, 255, 0.03);
        --glass-border: rgba(255, 255, 255, 0.08);
        --glass-hover: rgba(255, 255, 255, 0.06);

        --font-primary: "Inter", system-ui, -apple-system, sans-serif;
    }

    /* Reset Básico */
    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    body {
        margin: 0;
        min-height: 100vh;
        background-color: var(--bg-color);
        font-family: var(--font-primary);
        color: var(--text-pure);
        overflow: hidden; /* Evita scroll en vista base */
        display: flex;
        justify-content: center;
        align-items: center;

        /* 1️⃣ Fondo Gradiente Oscuro Radial + Linear */
        background-image:
            radial-gradient(
                circle at 15% 50%,
                rgba(112, 0, 255, 0.12),
                transparent 35%
            ),
            radial-gradient(
                circle at 85% 30%,
                rgba(0, 240, 255, 0.12),
                transparent 35%
            ),
            linear-gradient(180deg, var(--bg-color) 0%, #03060c 100%);
    }

    /* Contenedor Principal Centrado */
    .maintenance-container {
        position: relative;
        width: 100%;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        padding: 2rem;
        z-index: 1;
    }

    /* Destello de fondo tecnológico */
    .glow-effect {
        position: absolute;
        width: 60vw;
        height: 60vw;
        max-width: 800px;
        max-height: 800px;
        background: radial-gradient(
            circle,
            rgba(0, 176, 255, 0.04) 0%,
            rgba(112, 0, 255, 0.03) 40%,
            transparent 70%
        );
        border-radius: 50%;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: -1;
        pointer-events: none;
        filter: blur(50px);
    }

    .content {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        max-width: 800px;
        width: 100%;
    }

    /* 2️⃣ Logo Arriba + Floating */
    .logo-container {
        margin-bottom: 3.5rem;
    }

    .floating-logo {
        height: 100px;
        max-width: 100%;
        object-fit: contain;
        animation: float 6s ease-in-out infinite;
        filter: drop-shadow(0 10px 20px rgba(0, 0, 0, 0.5));
    }

    @media (min-width: 768px) {
        .floating-logo {
            height: 120px;
        }
    }

    /* 3️⃣ Texto Principal & Descripción */
    .main-content {
        width: 100%;
        max-width: 600px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.5rem;
    }

    h1 {
        font-size: clamp(2rem, 5vw, 3rem);
        font-weight: 700;
        line-height: 1.15;
        letter-spacing: -0.03em;
        margin: 0;
    }

    .gradient-text {
        /* Texto parcialmente con degradado tecnológico */
        background: linear-gradient(
            135deg,
            #ffffff 0%,
            #ffffff 45%,
            var(--brand-teal) 85%,
            var(--brand-blue) 100%
        );
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        color: transparent;
    }

    .description {
        font-size: clamp(1rem, 2.5vw, 1.125rem);
        color: var(--text-muted);
        line-height: 1.6;
        max-width: 460px;
        margin: 0 auto;
    }

    /* 5️⃣ Barra de Progreso */
    .progress-section {
        width: 100%;
        max-width: 420px;
        margin: 2.5rem 0;
    }

    .progress-header {
        display: flex;
        justify-content: space-between;
        margin-bottom: 0.85rem;
        font-size: 0.75rem;
        font-weight: 600;
        color: var(--text-muted);
        text-transform: uppercase;
        letter-spacing: 0.1em;
    }

    .progress-percentage {
        color: var(--brand-blue);
        font-weight: 700;
    }

    .progress-track {
        width: 100%;
        height: 6px;
        background: var(--glass-bg);
        border: 1px solid var(--glass-border);
        border-radius: 9999px;
        overflow: hidden;
        position: relative;
    }

    .progress-bar {
        position: absolute;
        top: 0;
        left: 0;
        height: 100%;
        width: 0%;
        background: linear-gradient(
            90deg,
            var(--brand-purple),
            var(--brand-blue)
        );
        border-radius: 9999px;
        box-shadow: 0 0 15px rgba(0, 240, 255, 0.4);
        /* Animación suave de llenado */
        animation: fill-progress 2.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        animation-delay: 1s;
    }

    /* 6️⃣ Botones de Contacto (Pill buttons glass styling) */
    .actions {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
        width: 100%;
        margin-top: 1rem;
    }

    .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        padding: 0.85rem 1.75rem;
        border-radius: 9999px; /* Estilo pill */
        font-size: 0.9375rem;
        font-weight: 500;
        text-decoration: none;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        cursor: pointer;
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
    }

    .btn svg {
        transition: transform 0.3s ease;
    }

    .btn:hover svg {
        transform: scale(1.1);
    }

    /* Estilo Semi-transparente / Glass */
    .btn-glass {
        background: var(--glass-bg);
        border: 1px solid var(--glass-border);
        color: var(--text-pure);
    }

    .btn-glass:hover {
        background: var(--glass-hover);
        border-color: rgba(255, 255, 255, 0.15);
        transform: translateY(-3px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    }

    /* Estilo Principal (Gradient button) */
    .btn-primary {
        background: linear-gradient(
            135deg,
            rgba(255, 255, 255, 0.05),
            rgba(255, 255, 255, 0.01)
        );
        color: white;
        border: 1px solid rgba(255, 255, 255, 0.15);
        position: relative;
        overflow: hidden;
    }

    .btn-primary::before {
        content: "";
        position: absolute;
        inset: 0;
        background: linear-gradient(
            135deg,
            var(--brand-purple),
            var(--brand-teal)
        );
        opacity: 0.15;
        transition: opacity 0.3s ease;
    }

    .btn-primary:hover {
        transform: translateY(-3px);
        border-color: rgba(0, 240, 255, 0.4);
        box-shadow: 0 10px 25px rgba(0, 240, 255, 0.15);
    }

    .btn-primary:hover::before {
        opacity: 0.3;
    }

    /* 💡 Keyframes & Animations */
    @keyframes float {
        0%,
        100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-12px);
        }
    }

    @keyframes fill-progress {
        0% {
            width: 0%;
        }
        100% {
            width: 70%;
        } /* Barra a 70% */
    }

    /* Utilitarios de animación de entrada */
    .fade-in {
        opacity: 0;
        animation: fadeIn 1s ease-out forwards;
    }

    .fade-in-up {
        opacity: 0;
        transform: translateY(20px);
        animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    }

    .delay-1 {
        animation-delay: 0.1s;
    }
    .delay-2 {
        animation-delay: 0.3s;
    }
    .delay-3 {
        animation-delay: 0.5s;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }

    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Responsive y ajustes adicionales */
    @media (max-width: 640px) {
        .maintenance-container {
            padding: 1.5rem;
            justify-content: space-evenly;
        }

        .actions {
            flex-direction: column;
            width: 100%;
            max-width: 320px;
        }

        .btn {
            width: 100%;
        }
    }

    /* Pequeña corrección de altura en móviles muy pequeños para evitar overflow: hidden bloqueando contenido */
    @media (max-height: 700px) {
        body {
            overflow-y: auto;
            align-items: flex-start;
        }
        .maintenance-container {
            min-height: auto;
            padding: 3rem 1.5rem;
        }
    }
</style>
