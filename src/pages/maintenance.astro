---
// src/pages/maintenance.astro
---

<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Mantenimiento | PARRA</title>
        <meta
            name="description"
            content="Estamos trabajando en mejoras importantes para ofrecerte un mejor rendimiento. Volveremos muy pronto."
        />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="theme-color" content="#000000" />

        <!-- Bold athletic fonts (Bebas Neue for strong uppercase headings, Inter for body) -->
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:wght@400;500;600;700&display=swap"
            rel="stylesheet"
        />
    </head>
    <body>
        <div class="maintenance-container">
            <!-- Subtle green center glow -->
            <div class="glow-bg"></div>

            <div class="content">
                <!-- Bold Logo -->
                <div class="logo-container fade-in">
                    <span class="logo-text">PARRA</span>
                </div>

                <main class="main-content fade-in delay-1">
                    <h1>
                        ESTAMOS MEJORANDO <span class="accent-text"
                            >LA EXPERIENCIA</span>
                    </h1>
                    <p class="description">
                        Estamos trabajando en mejoras importantes para ofrecerte
                        un mejor rendimiento. Volveremos muy pronto.
                    </p>

                    <!-- Progress Section -->
                    <div class="progress-section fade-in delay-2">
                        <div class="progress-header">
                            <span class="progress-label"
                                >ACTUALIZACIÓN EN CURSO</span
                            >
                            <span class="progress-percentage">70%</span>
                        </div>
                        <div
                            class="progress-track"
                            role="progressbar"
                            aria-valuenow="70"
                            aria-valuemin="0"
                            aria-valuemax="100"
                        >
                            <div class="progress-bar"></div>
                        </div>
                    </div>

                    <!-- High-contrast Buttons -->
                    <div class="actions fade-in delay-3">
                        <a
                            href="https://wa.me/tunumerodewhatsapp"
                            target="_blank"
                            rel="noopener noreferrer"
                            class="btn"
                            aria-label="Contactar por WhatsApp"
                        >
                            <svg
                                viewBox="0 0 24 24"
                                width="22"
                                height="22"
                                stroke="currentColor"
                                stroke-width="2.5"
                                fill="none"
                                stroke-linecap="square"
                                stroke-linejoin="miter"
                                ><path
                                    d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"
                                ></path></svg
                            >
                            WHATSAPP
                        </a>
                        <a
                            href="mailto:contacto@tuemail.com"
                            class="btn"
                            aria-label="Contactar por Email"
                        >
                            <svg
                                viewBox="0 0 24 24"
                                width="22"
                                height="22"
                                stroke="currentColor"
                                stroke-width="2.5"
                                fill="none"
                                stroke-linecap="square"
                                stroke-linejoin="miter"
                                ><path
                                    d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"
                                ></path><polyline points="22,6 12,13 2,6"
                                ></polyline></svg
                            >
                            EMAIL
                        </a>
                        <a
                            href="/"
                            class="btn btn-primary"
                            aria-label="Volver al inicio"
                        >
                            <svg
                                viewBox="0 0 24 24"
                                width="22"
                                height="22"
                                stroke="currentColor"
                                stroke-width="2.5"
                                fill="none"
                                stroke-linecap="square"
                                stroke-linejoin="miter"
                                ><path
                                    d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"
                                ></path><polyline points="9 22 9 12 15 12 15 22"
                                ></polyline></svg
                            >
                            VOLVER AL INICIO
                        </a>
                    </div>
                </main>
            </div>
        </div>
    </body>
</html>

<style>
    /* PARRA Brand Variables */
    :root {
        --color-black: #000000;
        --color-neon-green: #39ff14;
        --color-dark-gray: #1a1a1a;
        --color-charcoal: #2d2d2d;
        --color-text-main: #ffffff;
        --color-text-muted: #a0a0a0;

        --font-heading: "Bebas Neue", Impact, sans-serif;
        --font-body: "Inter", system-ui, sans-serif;
    }

    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    body {
        margin: 0;
        height: 100vh;
        background-color: var(--color-black);
        font-family: var(--font-body);
        color: var(--color-text-main);
        overflow: hidden; /* No scroll vertically */
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .maintenance-container {
        position: relative;
        width: 100%;
        height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        padding: 2rem;
        z-index: 1;
    }

    /* Subtle neon green background glow */
    .glow-bg {
        position: absolute;
        width: 80vw;
        height: 80vw;
        max-width: 800px;
        max-height: 800px;
        background: radial-gradient(
            circle,
            rgba(57, 255, 20, 0.05) 0%,
            transparent 65%
        );
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: -1;
        pointer-events: none;
    }

    .content {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        max-width: 800px;
        width: 100%;
    }

    /* Brand Logo Text */
    .logo-container {
        margin-bottom: 3.5rem;
    }

    .logo-text {
        font-family: var(--font-heading);
        font-size: clamp(4rem, 10vw, 7rem);
        font-weight: 400; /* Bebas Neue is bold natively */
        letter-spacing: 0.05em;
        color: var(--color-neon-green);
        line-height: 1;
        text-shadow: 0 0 30px rgba(57, 255, 20, 0.2);
    }

    /* Primary Heading & Subtext */
    .main-content {
        width: 100%;
        max-width: 760px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.5rem;
    }

    h1 {
        font-family: var(--font-heading);
        font-size: clamp(3rem, 7vw, 5.5rem);
        line-height: 1.05;
        letter-spacing: 0.03em;
        margin: 0;
        text-transform: uppercase;
        color: var(--color-text-main);
    }

    .accent-text {
        color: var(--color-neon-green);
    }

    .description {
        font-size: clamp(1rem, 2.5vw, 1.25rem);
        color: var(--color-text-muted);
        line-height: 1.6;
        max-width: 600px;
        margin: 0 auto;
        font-weight: 400;
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    /* Progress Mechanics */
    .progress-section {
        width: 100%;
        max-width: 500px;
        margin: 2.5rem 0;
    }

    .progress-header {
        display: flex;
        justify-content: space-between;
        margin-bottom: 0.6rem;
        font-family: var(--font-heading);
        font-size: clamp(1.2rem, 3vw, 1.5rem);
        color: var(--color-text-muted);
        letter-spacing: 0.1em;
    }

    .progress-percentage {
        color: var(--color-neon-green);
    }

    .progress-track {
        width: 100%;
        height: 6px; /* Thin, strong line */
        background-color: var(--color-dark-gray);
        position: relative;
    }

    .progress-bar {
        position: absolute;
        top: 0;
        left: 0;
        height: 100%;
        width: 0%;
        background-color: var(--color-neon-green);
        box-shadow: 0 0 15px rgba(57, 255, 20, 0.4);
        animation: fill-progress 1.5s cubic-bezier(0.16, 1, 0.3, 1) forwards
            0.5s;
    }

    /* Action Buttons - Athletic & Rectangular */
    .actions {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
        width: 100%;
        margin-top: 1.5rem;
    }

    .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.75rem;
        padding: 1.1rem 2rem;
        font-family: var(--font-heading);
        font-size: clamp(1.2rem, 3vw, 1.4rem);
        letter-spacing: 0.05em;
        text-decoration: none;
        text-transform: uppercase;
        transition: all 0.2s ease-out;
        cursor: pointer;
        /* Core Athletic Button Styles */
        background-color: var(--color-black);
        color: var(--color-neon-green);
        border: 2px solid var(--color-neon-green);
        border-radius: 0; /* Sharp edges */
    }

    .btn:hover {
        background-color: var(--color-neon-green);
        color: var(--color-black);
        box-shadow: 0 0 20px rgba(57, 255, 20, 0.4);
        transform: translateY(-2px);
    }

    /* Keep the primary button visually identical or slightly accented if needed */
    .btn-primary {
        background-color: var(--color-neon-green);
        color: var(--color-black);
    }

    .btn-primary:hover {
        background-color: var(--color-black);
        color: var(--color-neon-green);
        box-shadow: 0 0 20px rgba(57, 255, 20, 0.4);
    }

    /* Minimal Powerful Fade Animations */
    .fade-in {
        opacity: 0;
        animation: fadeIn 0.8s ease-out forwards;
    }

    .delay-1 {
        animation-delay: 0.2s;
    }
    .delay-2 {
        animation-delay: 0.4s;
    }
    .delay-3 {
        animation-delay: 0.6s;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(15px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes fill-progress {
        0% {
            width: 0%;
        }
        100% {
            width: 70%;
        }
    }

    /* Responsive Mobile Layout */
    @media (max-width: 640px) {
        .actions {
            flex-direction: column;
            width: 100%;
            max-width: 340px;
        }

        .btn {
            width: 100%;
        }

        .logo-container {
            margin-bottom: 2rem;
        }

        .progress-section {
            padding: 0; /* Inherits container padding effectively */
        }
    }
</style>
